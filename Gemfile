source "https://rubygems.org"

# Keeps local builds aligned with GitHub Pages supported Jekyll/plugin versions.
gem "github-pages", group: :jekyll_plugins

# Needed by Ruby 3+ for local Jekyll serve in many environments.
gem "webrick", "~> 1.8"
